import { createStore } from 'redux'

/**
 *  User 
 *          User function which control and define the behavior of the User 
 *          and the scope of the user threw the app 
 *  @public
 */
export const user = (() => {
    // Defining the store using redux 
    let store = createStore()

})()